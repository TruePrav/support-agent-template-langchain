# Core AI framework
# Pinned to 0.3.x -- LangChain 1.x has breaking API changes incompatible with this template
langchain>=0.3.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
langchain-openai>=0.2.0,<1.0.0
langchain-anthropic>=0.2.0,<1.0.0
langchain-google-genai>=2.0.0,<3.0.0
langchain-community>=0.3.0,<1.0.0
langchain-postgres>=0.0.17,<1.0.0

# Environment / config
python-dotenv

# Telegram channel
python-telegram-bot>=20.0,<22.0

# Web chat channel (FastAPI + server)
fastapi
uvicorn
pydantic

# Database (Postgres memory)
psycopg[binary]

# Meta channel (WhatsApp / Instagram / Messenger)
httpx
